# ابدأ من هنا — حزمة Cursor لمبادرة العجاونة

هذه الحزمة تحوّل المواصفة (الإصدار 1.5) إلى سياق ومهام يفهمها Cursor.

## المحتويات
- `AGENTS.md` — تعليمات المشروع للوكيل.
- `.cursor/rules/*.mdc` — قواعد Cursor (مشروع، Laravel، واجهة RTL، أمان، اختبارات، Git).
- `docs/SPEC.md` — المواصفة الكاملة (المرجع الأعلى).
- `docs/DECISIONS.md` — القرارات التقنية والبنود المفتوحة.
- `docs/DESIGN-TOKENS.md` — الألوان والخطوط وإعداد Tailwind.
- `docs/DATA-TRANSFER-REGISTER.md` — قالب سجل نقل البيانات.
- `docs/prototype/` — النموذج البصري الأول (الشكل فقط، منطقه قديم).
- `tasks/` — 22 مهمة مرقّمة (T00 إلى T21) مع فهرس.
- `.github/pull_request_template.md` — قالب الـ PR.

## المتطلبات على جهازك
Git · PHP 8.3 أو أحدث · Composer · Node.js · Docker (لقاعدة PostgreSQL وRedis محليًا) · Cursor.

## الترتيب داخل المشروع
فك الضغط **في جذر المستودع مباشرة**، فتصبح الملفات هكذا:

```text
my-repo/                  ← جذر المستودع
├── START-HERE.md
├── AGENTS.md
├── .cursor/rules/        ← مجلد مخفي، تأكد من ظهوره
├── .github/              ← مجلد مخفي
│   └── pull_request_template.md
├── docs/
│   ├── SPEC.md
│   ├── DECISIONS.md
│   ├── DESIGN-TOKENS.md
│   ├── DATA-TRANSFER-REGISTER.md
│   └── prototype/
└── tasks/
    ├── 00-INDEX.md
    └── T00 … T21
```

بعد المهمة T00 يُضاف مشروع Laravel إلى الجذر نفسه (`app/`, `routes/`, `composer.json`…) بجانب هذه الملفات، ولا تتعارض معها.

## البدء
1. أنشئ مستودعًا **خاصًا** فارغًا على GitHub، واستنسخه: `git clone <رابط المستودع> my-repo`.
2. فك الحزمة في الجذر: `cd my-repo && unzip ../ajawna-cursor-pack.zip`. **إن أنشأ برنامج فك الضغط مجلدًا إضافيًا فانقل محتوياته إلى الجذر** حتى يكون `AGENTS.md` في الجذر مباشرة.
3. أول commit على `main` (قبل تفعيل حمايته): `git add . && git commit -m "chore: add cursor pack" && git push`.
4. افتح المجلد في Cursor، وأنشئ فرع المهمة: `git switch -c task/T00-setup`.
5. في Cursor (وضع الوكيل) استخدم هذا الطلب مع تغيير رقم المهمة:

```text
اقرأ AGENTS.md ثم docs/SPEC.md ثم tasks/T00-setup.md.
نفّذ هذه المهمة فقط في فرعها، وتحقق من واجهات المكتبات عبر Laravel Boost.
اكتب الاختبارات المطلوبة، وشغّل composer lint وanalyse وtest، ثم لخّص ما تم وما بقي وأي أسئلة مفتوحة.
لا تخرج عن نطاق المهمة ولا تختلق متطلبات.
```

6. راجع الفروقات (diff) بنفسك، وشغّل الاختبارات، وافتح PR، وادمج بعد نجاح CI.
7. انتقل للمهمة التالية من `tasks/00-INDEX.md`.

## نصائح
- **مهمة واحدة في كل مرة.** لا تعطِ الوكيل مهمتين معًا.
- عند التعارض أو الغموض يكتب الوكيل سؤالًا في الـ PR ويتوقف. ارجع للمواصفة أو إليّ لتعديلها قبل المتابعة.
- الأمن أولًا: لا أسرار ولا بيانات حقيقية في المستودع أو في لقطات الشاشة.
- المراجعة القانونية (الترخيص ونقل البيانات) شرط للإطلاق العام، وليست جزءًا من الكود.
