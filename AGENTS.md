# مبادرة العجاونة — تعليمات الوكيل (Cursor)

## المشروع
نظام لدعم زواج مستفيدين متزوجين مسجّلين. يختار المبادر مستفيدًا، فيحوّل إلى حسابه البنكي مباشرة، ثم يرفع إيصاله فيدخل الإحصائيات فورًا. لوحة إدارة بمشرفين وصلاحيات دقيقة. **المال لا يمرّ عبر الموقع إطلاقًا.**

## اقرأ أولًا (بالترتيب)
1. `docs/SPEC.md` — المتطلبات، وهي المرجع الأعلى.
2. `docs/DECISIONS.md` — القرارات التقنية المعتمدة.
3. `docs/DESIGN-TOKENS.md` — الألوان والخطوط.
4. ملف المهمة الحالية في `tasks/`.

## الحزمة التقنية (لا تغيّرها دون موافقة)
PHP 8.3+ · Laravel 13 · Livewire 4 · Alpine.js · Tailwind CSS 4 · Filament 5 · PostgreSQL · Redis · spatie/laravel-permission · Pest 5 · Laravel Pint · Larastan.
- **تحقق من واجهات المكتبات في وثائق الإصدار الصحيح** عبر Laravel Boost (`search-docs`) قبل كتابة الكود. Filament 5 يعتمد على Livewire 4 وواجهة Schemas، ولا تستخدم واجهات Filament 3 القديمة.
- لا React ولا Inertia ولا مكتبة واجهة جديدة دون موافقة مكتوبة.

## قواعد ذهبية
1. **مهمة واحدة = فرع واحد = PR واحد.** لا تخرج عن نطاق المهمة.
2. لا تختلق متطلبات. الغموض يُكتب في PR كسؤال.
3. **لا بريد إلكتروني في أي مكان** (لا عمود ولا حقل ولا إرسال). لا رسائل نصية ولا إرسال تلقائي للجوال. الروابط تُفتح بواسطة المشرف عبر `https://wa.me/...`.
4. **رقم الجوال:** سعودي فقط، يُخزَّن بصيغة `+9665XXXXXXXX` عبر `App\Support\SaudiPhone` (لا تكتب التحويل يدويًا في مكان آخر).
5. **الاسم الكامل:** أربع كلمات فأكثر. رسالة الإرشاد في الحقل: "اكتب اسمك كاملًا" فقط.
6. **المبالغ:** `decimal(12,2)` ولا تستخدم float. العملة من الإعدادات (الافتراضي ر.س).
7. **التواريخ:** تُخزَّن ميلادية (`date`)، وتُعرض هجرية (أم القرى) وميلادية معًا عبر `App\Support\HijriDate`. العرض بتوقيت `Asia/Riyadh`.
8. **الحوالة تُحتسب فور رفع الإيصال.** لا توجد حالة "قيد المراجعة" ولا "رفض". المراجعة اختيارية للمطابقة (انظر المواصفة).
9. **أسماء المبادرين المحوِّلين:** الاسم الأول فقط في أي قائمة عامة. **أسماء المستفيدين:** كاملة دائمًا ولا تُقتطع.
10. **الحساب البنكي (آيبان/رقم حساب):** يُعرض فقط من قاعدة المستفيدين عبر كتلة/صفحة نظامية، ولا يُكتب في نص حر أبدًا (انظر `30-security-privacy`).
11. **الصلاحيات:** فحص على الخادم دائمًا (Policies/Gates) بمفاتيح الصلاحية من المواصفة. إخفاء الأزرار وحده لا يكفي. المدير يملك كل شيء ضمنيًا.
12. **لا أسرار ولا بيانات حقيقية في المستودع.** كل إعداد في `.env` ومعه `.env.example`.
13. لا خدمة خارجية جديدة (تحليلات، خطوط CDN، مراقبة أخطاء...) دون موافقة. الخطوط مستضافة محليًا.

## مسرد المصطلحات
| العربية | في الكود |
|---|---|
| المبادرة / المستفيد | `Beneficiary` (مبادرة كل مستفيد: `active` متاحة أو `closed` مغلقة) |
| المبادر | `User` بدور `user` |
| المشرف | `User` بدور `supervisor` |
| المدير | `User` بدور `admin` |
| الحوالة | `Transfer` |
| طلب الاستعادة | `PasswordResetRequest` |

## اللغة والواجهة
- نصوص الواجهة عربية بالكامل، والمعرّفات في الكود إنجليزية. ملفات الترجمة في `lang/ar`.
- RTL أولًا: استخدم الخصائص المنطقية (`ms-*`/`me-*`/`ps-*`/`pe-*`، `text-start`/`text-end`)، ولا تكتب `left`/`right` ثابتة.
- الأرقام الحساسة (جوال، آيبان، رقم حساب) تُعرض `dir="ltr"` بأرقام لاتينية.
- الجوال أولًا، أصغر عرض للاختبار 360px، وأهداف اللمس 44px على الأقل.

## تعريف الإنجاز (لكل مهمة)
- [ ] معايير القبول في ملف المهمة كلها متحققة.
- [ ] اختبارات Pest للحالات الطبيعية والحدية **ولحالات المنع (403)**.
- [ ] `composer lint` و`composer analyse` و`composer test` تنجح.
- [ ] لقطات شاشة (جوال 360px وكمبيوتر 1280px) لأي مهمة فيها واجهة.
- [ ] لا تغييرات خارج نطاق المهمة، ولا ملفات مؤقتة.
- [ ] وصف PR: ما تم، ما لم يتم، الأسئلة المفتوحة.
